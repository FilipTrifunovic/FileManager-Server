Run "docker-compose up" to start the project some times the backend will fail to authenticate when applying migrations just run the main container again
Configuration is set up for easyer development
DB migration will run when the project starts this is also just for development and should't be used in production